# `cli-setup --verbose`, `cli-setup -v`

```bash
cli-setup --verbose
```

```bash
cli-setup -v
```

A global flag the dispatcher accepts today. It will turn on detailed output once
the profile commands land; with no command to run, it prints the help.

- Exit code: `0`.
