# `cli-setup --help`, `cli-setup -h`, and no arguments

```bash
cli-setup --help
```

```bash
cli-setup -h
```

```bash
cli-setup
```

All three print usage — the invocation form, the (planned) commands, and the
global options. Running `cli-setup` with no arguments prints the same help.

- Exit code: `0`.
