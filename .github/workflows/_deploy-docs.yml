name: deploy-docs
on:
  workflow_call:
permissions:
  contents: read
  pages: write
  id-token: write
jobs:
  deploy-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - name: deploy-docs (placeholder)
        run: |
          echo "deploy-docs placeholder - slice #9: mdBook build + deploy to GitHub Pages"
