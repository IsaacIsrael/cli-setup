name: gate-branch-lint
on:
  workflow_call:
permissions:
  contents: read
jobs:
  branch-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - name: branch-lint (placeholder)
        run: |
          echo "branch-lint placeholder - slice #5: branch-name lint (CI backstop for the Lefthook pre-push hook)"
