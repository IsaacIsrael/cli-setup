#!/usr/bin/env bash
# cli-setup entrypoint — placeholder. The real command dispatcher (flag parsing,
# --version / --help, subcommands) lands in #8; for now it prints its name so the
# ShellSpec harness can exercise the real entrypoint end to end.
set -euo pipefail

echo "broken-on-purpose"
